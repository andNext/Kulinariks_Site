<% User.all.each do |f| %>
<% @adt  = f.adts.build %>
<% @feed_items = f.feed.paginate(page: params[:page]) %>
	<% if @feed_items.any? %>
	  <ol class="adts">
	    <%= render partial: 'shared/feed_item', collection: @feed_items %>
	  </ol>
	  <%= will_paginate @feed_items %>
	<% end %>
<% end %>
