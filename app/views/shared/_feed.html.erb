<!-- Партиал потока объявлений одного пользователя-->
<% if @feed_items.any? %>
  <ol class="adts">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>